<div class="l-content {{ modifier }}">
  {% if constrain %}
    <div class="{{ constrain }}">
  {% endif %}

  {% block content %}
    {{ content|default('<div><p style="border: 1px dashed #333; color:#333; padding:1rem; margin:0;">Main Content</p></div>') }}
  {% endblock %}

  {% if constrain %}
    </div>
  {% endif %}
</div>
