{{ attach_library('ohana/nav') }}

<nav class="l-nav" role="navigation">
  <h2 class="visually-hidden">{{ label }}</h2>
  {% block content %}
    {{ content|default('<div><p style="border: 1px dashed #333; color:#333; padding:1rem; margin:0;">Menu Content</p></div>') }}
  {% endblock %}
</nav>
