<figure class="figure {{ modifier }}"{% if caption is defined %} role="group"{% endif %}>
  <div class="figure__media">{{ media }}</div>
  {% if caption is defined %}
    <figcaption class="figure__caption">{{ caption }}</figcaption>
  {% endif %}
</figure>
